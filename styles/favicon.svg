<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
    <styles>
      .orb{
      fill: #0B0026;
      filter: drop-shadow(0 0 6px #1B2600);
      animation: p 2s infinite ease-in-out;
      }
      @keyframes p{
      0%{
      transform: scale(1);
      opacity: 1;}
      50%{
      transform: scale(1.15);
      opacity: 0.7;}
      100%{
      transform: scale(1);
      opacity: 1;
      }
      }
    </styles>
      <circle class="orb" cx="32" cy="32" r="16" />
</svg>
